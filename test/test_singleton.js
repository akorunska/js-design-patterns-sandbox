const assert = require("assert");

describe("Singleton", function () {
  describe("#singletonMethod()", function () {
    it("should do something", function () {
      assert.ok("foobar");
    });
  });
});
